diff --git a/kernel/Kconfig b/kernel/Kconfig
index 45f87e42..b0c2a5fe 100644
--- a/kernel/Kconfig
+++ b/kernel/Kconfig
@@ -77,19 +77,17 @@ config KSU_MULTI_MANAGER_SUPPORT
 	help
 		Enable multi KernelSU manager support
 
-choice
-	prompt "KernelSU Hooking Method"
-	depends on KSU
-	default KSU_TRACEPOINT_HOOK
-	help
-	  Select the hooking method for KernelSU.
-	  Note: Tracepoint Syscall Redirect hook is only supported for GKI2(5.10+)
+# Hooking methods are independent (no mutual-exclusive choice), matching
+# KSU Next kernels: MANUAL_HOOK and SUSFS can be enabled together. This is
+# required for stable operation on non-GKI 4.19 QTI kernels (adb_root,
+# selinux_hide and zygisk all work with both enabled).
 
 config KSU_TRACEPOINT_HOOK
 	bool "Tracepoint Syscall Redirect"
+	depends on KSU
 	help
-	  The standard, upstream method for modern kernels. 
-	  Utilizes GKI tracepoints for syscall redirection. 
+	  The standard, upstream method for modern kernels.
+	  Utilizes GKI tracepoints for syscall redirection.
 	  The implementation is only for devices with GKI2 support.
 	  Support for kernel 5.10+
 
@@ -97,7 +95,7 @@ config KSU_MANUAL_HOOK
 	bool "Manually hook KernelSU"
 	depends on KSU != m
 	help
-	  If enabled, it hooks the required KernelSU syscalls 
+	  If enabled, it hooks the required KernelSU syscalls
 	  with a manually-patched function.
 	  Support for kernel 3.4+
 
@@ -107,11 +105,9 @@ config KSU_SUSFS
 	help
 	  Patch and Enable SUSFS to kernel with KernelSU.
 	  SuSFS Officially support kernel 5.10+
-	  This KSU driver support it for 4.3+ in KSU side, 
+	  This KSU driver support it for 4.3+ in KSU side,
 	  But the kernel side susfs compatibility MUST completed by yourself.
 
-endchoice
-
 menu "KernelSU - Manual Hook Auto hook"
     depends on KSU_MANUAL_HOOK
 
diff --git a/kernel/feature/sucompat.c b/kernel/feature/sucompat.c
index 4fd64343..0f7648ee 100644
--- a/kernel/feature/sucompat.c
+++ b/kernel/feature/sucompat.c
@@ -482,7 +482,7 @@ int ksu_handle_execveat_sucompat(int *fd, struct filename **filename_ptr, void *
 #endif
 #endif
 
-#ifdef CONFIG_KSU_SUSFS
+#if defined(CONFIG_KSU_SUSFS) && LINUX_VERSION_CODE >= KERNEL_VERSION(6, 1, 0)
 int ksu_handle_faccessat(int *dfd, struct filename **filename, int *mode, int *__unused_flags)
 {
     const struct cred *old_cred;
@@ -556,7 +556,7 @@ int ksu_handle_faccessat(int *dfd, const char __user **filename_user, int *mode,
 }
 #endif
 
-#ifdef CONFIG_KSU_SUSFS
+#if defined(CONFIG_KSU_SUSFS) && LINUX_VERSION_CODE >= KERNEL_VERSION(6, 1, 0)
 int ksu_handle_stat(int *dfd, struct filename **filename, int *flags)
 {
     const struct cred *old_cred;
diff --git a/kernel/feature/sucompat.h b/kernel/feature/sucompat.h
index 0c6365c0..6d591e91 100644
--- a/kernel/feature/sucompat.h
+++ b/kernel/feature/sucompat.h
@@ -14,13 +14,13 @@ void ksu_sucompat_init(void);
 void ksu_sucompat_exit(void);
 
 // Handler functions exported for hook_manager
-#ifdef CONFIG_KSU_SUSFS
+#if defined(CONFIG_KSU_SUSFS) && LINUX_VERSION_CODE >= KERNEL_VERSION(6, 1, 0)
 int ksu_handle_faccessat(int *dfd, struct filename **filename, int *mode, int *__unused_flags);
 int ksu_handle_stat(int *dfd, struct filename **filename, int *flags);
 #else
 int ksu_handle_faccessat(int *dfd, const char __user **filename_user, int *mode, int *__unused_flags);
 int ksu_handle_stat(int *dfd, const char __user **filename_user, int *flags);
-#endif // #ifdef CONFIG_KSU_SUSFS
+#endif // #if defined(CONFIG_KSU_SUSFS) && LINUX_VERSION_CODE >= KERNEL_VERSION(6, 1, 0)
 
 #ifdef CONFIG_KSU_TRACEPOINT_HOOK
 #include <asm/current.h>
diff --git a/kernel/runtime/ksud_integration.c b/kernel/runtime/ksud_integration.c
index 2ddcf312..2d90ffe2 100644
--- a/kernel/runtime/ksud_integration.c
+++ b/kernel/runtime/ksud_integration.c
@@ -98,6 +98,9 @@ static void stop_execve_hook(void);
 #elif defined(CONFIG_KSU_SUSFS)
     DEFINE_STATIC_KEY_TRUE(ksu_is_init_rc_hook_enabled);
     DEFINE_STATIC_KEY_TRUE(ksu_is_input_hook_enabled);
+    /* When MANUAL_HOOK and SUSFS are both enabled, lsm_hooks.o still
+     * references ksu_init_rc_hook; provide it here too. */
+    DEFINE_STATIC_KEY_TRUE(ksu_init_rc_hook);
 
     // use define to avoid ifdef
     #define ksu_init_rc_hook_inactive() (!static_branch_likely(&ksu_is_init_rc_hook_enabled))
